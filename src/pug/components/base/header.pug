mixin header(props = {})
  -
    const current = props.currentUrl

  header.header(class=headerMods)
    .width
      .header__core
        each item in dataMenu
          a(
            href=item.href
            class=item.href === current && 'is-active'
          )!=item.title
